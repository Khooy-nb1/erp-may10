const { costingDetail, costingFilters, listCosting } = require('../services/costingService');
function invalid(message){const error=new Error(message);error.status=400;throw error;}function text(q,k,m=200){const v=q[k];if(v===undefined)return'';if(typeof v!=='string'||v.length>m)invalid(`Tham số ${k} không hợp lệ.`);return v.trim();}function id(v){if(!/^[1-9]\d{0,18}$/.test(v))invalid('ID không hợp lệ.');return v;}function day(q,k){const v=text(q,k,10);if(v&&(!/^\d{4}-\d{2}-\d{2}$/.test(v)||Number.isNaN(Date.parse(v))))invalid(`Ngày ${k} không hợp lệ.`);return v;}function integer(q,k,f,m){const v=text(q,k,10);if(!v)return f;if(!/^\d+$/.test(v)||+v<1||+v>m)invalid(`Tham số ${k} không hợp lệ.`);return +v;}
async function getCosting(req,res){const allowed=['q','objectId','status','from','to','page','pageSize'];if(Object.keys(req.query).some(k=>!allowed.includes(k)))invalid('Tham số không được hỗ trợ.');const from=day(req.query,'from'),to=day(req.query,'to'),objectId=text(req.query,'objectId',20),status=text(req.query,'status',20);if(from&&to&&from>to)invalid('Ngày bắt đầu phải trước hoặc bằng ngày kết thúc.');if(status&&!['calculated','missing'].includes(status))invalid('Trạng thái không hợp lệ.');res.set('Cache-Control','no-store').json(await listCosting({q:text(req.query,'q'),objectId:objectId?id(objectId):'',status,from,to,page:integer(req.query,'page',1,100000),pageSize:integer(req.query,'pageSize',20,100)}));}
async function getCostingFilters(req,res){res.set('Cache-Control','no-store').json(await costingFilters());}
async function getCostingDetail(req,res){const data=await costingDetail(id(req.params.id));if(!data)return res.status(404).json({error:{message:'Không tìm thấy đối tượng tính giá thành.'}});res.set('Cache-Control','no-store').json({data});}

module.exports = { getCosting, getCostingFilters, getCostingDetail };
